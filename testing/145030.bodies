class AnyState
!!!160006.cpp!!!	Start(inout stm : Exit001) : void
  AnyState * st = _upper(stm);

  if (st != 0)
    st->Start(stm);
#ifdef VERBOSE_STATE_MACHINE
  else
    std::cout << "DEBUG : transition Start not expected" << std::endl;
#endif
!!!229126.cpp!!!	_completion(inout stm : Exit001) : bool
return true;
